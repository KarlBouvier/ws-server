/**
 * Client Prisma local — généré via `prisma generate` à partir de ./prisma/schema.prisma.
 * Sur Render : le script `postinstall` exécute automatiquement `prisma generate`
 * après `npm install`, ce qui produit le client dans node_modules/@prisma/client.
 */
import { PrismaClient } from "@prisma/client";

export const prisma = new PrismaClient();
